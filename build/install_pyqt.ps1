# PyQt is installed from build/requirements-windows.txt.
#
# This placeholder keeps older build scripts callable while DrumBurp finishes
# the migration to Python 3 and PyQt5.

Write-Host "PyQt5 is installed by pip requirements; no standalone installer needed."
